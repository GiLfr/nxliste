git init # Initialisation du dépôt Git
git flow init # Initialisation de Git flow dans le dépôt Git
py -m venv .venv # Virtualisation de l'environnement
av # Activation de la virtualisation de l'environnement (alias dans ../../.bashrc)
py -m pip install --upgrade pip # dernière version de pip
# pip install -r requirements.txt # si requirements présent, installation en venv des librairies python
# git remote add origin https://github.com/GiLfr/nxliste.git # s'il existe, déclaration du dépôt distant

## Autres infos, hors init
## rm -rf .git # pour supprimer le dépôt git, par exemple, marche avec n'importe quel répertoire (.venv, ...)
